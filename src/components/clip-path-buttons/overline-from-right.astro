<button class="hvr-OverlineFromRight"><div class="button-inner">OverlineFromRight</div></button>

<style>
  button {
    appearance: none;
    border: 0;
    padding: 1em;
    font-size: 1em;
    cursor: pointer;
    background: #e1e1e1;
    color: #666;
    position: relative;
  }

  button::before {
    content: "";
    position: absolute;
    inset: 0;
    background: #e1e1e1;
    border-top: 4px solid #2098d1;
    transition-duration: 0.3s;
    z-index: 1;
  }

  .button-inner {
    position: relative;
    z-index: 2;
  }

  .hvr-OverlineFromRight::before {
    clip-path: inset(0 0 0 100%);
    transition-property: clip-path;
  }

  .hvr-OverlineFromRight:hover::before {
    clip-path: inset(0);
  }
</style>
