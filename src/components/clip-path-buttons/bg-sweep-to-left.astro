<button class="hvr-SweepToLeft"><div class="button-inner">SweepToLeft</div></button>

<style>
  button {
    appearance: none;
    border: 0;
    padding: 1em;
    font-size: 1em;
    cursor: pointer;
    background: #e1e1e1;
    position: relative;
    transition-duration: 0.3s;
  }

  button::before {
    content: "";
    position: absolute;
    inset: 0;
    background: #2098d1;
    transition-duration: 0.3s;
    z-index: 1;
  }

  .button-inner {
    position: relative;
    z-index: 2;
  }

  .hvr-SweepToLeft {
    color: #666;
    transition-property: color;
  }

  .hvr-SweepToLeft:hover {
    color: white;
  }

  .hvr-SweepToLeft::before {
    clip-path: inset(0 0 0 100%);
    transition-property: clip-path;
  }

  .hvr-SweepToLeft:hover::before {
    clip-path: inset(0);
  }
</style>
