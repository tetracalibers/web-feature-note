<button>Line2CornerToBorder</button>

<style>
  button {
    appearance: none;
    border: 0;
    background: #e1e1e1;
    padding: 1em;
    font-size: 1em;
    cursor: pointer;
    position: relative;
    color: #666;
  }

  button::before,
  button::after {
    content: "";
    position: absolute;
    inset: 0;
    border: 2px solid #2098d1;
    transition-property: clip-path;
    transition-duration: 0.3s;
  }

  button::before {
    border-right: 0;
    border-bottom: 0;
    clip-path: inset(0 80% 50% 0);
  }

  button::after {
    border-top: 0;
    border-left: 0;
    clip-path: inset(50% 0 0 80%);
  }

  button:hover::before,
  button:hover::after {
    clip-path: inset(0);
  }
</style>
