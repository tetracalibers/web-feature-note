<button class="hvr-UnderlineFromCenter"><div class="button-inner">UnderlineFromCenter</div></button>

<style>
  button {
    appearance: none;
    border: 0;
    padding: 1em;
    font-size: 1em;
    cursor: pointer;
    background: #e1e1e1;
    color: #666;
    position: relative;
  }

  button::before {
    content: "";
    position: absolute;
    inset: 0;
    background: #e1e1e1;
    border-bottom: 4px solid #2098d1;
    transition-duration: 0.3s;
    z-index: 1;
  }

  .button-inner {
    position: relative;
    z-index: 2;
  }

  .hvr-UnderlineFromCenter::before {
    clip-path: inset(0 50% 0 50%);
    transition-property: clip-path;
  }

  .hvr-UnderlineFromCenter:hover::before {
    clip-path: inset(0);
  }
</style>
