---
title: カードコンポーネントでつまみ食いCSS Tips
demo_link: /demo/cards
---

import EmbedOtherPage from "@/components/embed-other-page.astro"
import Card from "@/demo/topic/card.astro"
import OtherPageLink from "@/components/link/other-page-link.astro"

<OtherPageLink slugs={["demo", "cards"]}>デモをフルスクリーンで表示</OtherPageLink>

<EmbedOtherPage pagePath={frontmatter.demo_link}>
  <Card />
</EmbedOtherPage>

## Cardのベーススタイル

```html
<div class="Card">
  <div class="CardHeader">
    <img class="CardHeader__image" src={`http://placeimg.com/${width}/${height}/${category}`} alt=""
    />
    <h2 class="CardHeader__title">{title}</h2>
  </div>
  <div class="CardBody">
    <p class="CardBody__paragraph">{text}</p>
  </div>
  <div class="CardFooter">
    <div class="PublishDate IconText">
      <span class="IconText__icon PublishDate__icon"><ClockIcon /></span>
      <time class="IconText__text PublishDate__date" datetime="2022-12-03">2022/12/03</time>
    </div>
    <ul class="Avatars">
      { avatars.map(avatar => (
      <li class="Avatar">
        <img class="Avatar__thumb" src="{avatar}" alt="" loading="lazy" />
      </li>
      )) }
    </ul>
  </div>
</div>
```

## Cardの角にリボンを被せる

## Cardに波紋アニメーションを加える

## 複数Cardのレイアウト

<!--

## gridによる重ね合わせ配置

- 画像の上にテキストを重ねる（同セル配置）
- 角に装飾を加える（位置と広がりの制御）

## flexによるインライン配置

- アイコンとテキストを揃えて並べる
  - `flex-shrink: 0;`
  - `lh`と`rlh`

## 背景画像とテキストのコントラストの確保

- 背景画像とテキスト色のコントラストを動的に確保する
  - `filter`
  - `background-clip: text;`
  - `mix-blend-mode: difference;`
- テキストを読みやすくするための画像加工
  - 局所的なOverlay
    - 局所的に色がつくように`gradient`を重ねる
    - `mask-image`で局所的に透過させる
  - テキストの形に合わせて背景色をつける
    - `box-decoration-break: clone;`
- ダークモードに対応する
  - `prefers-color-scheme: dark;`
  - `filter: grayscale();`

## line-clampによるコンテンツの省略

- 長いテキストを省略する
  - `-webkit-line-clamp`

## border-radiusに代わるclip-path

- 波紋エフェクトをつける
  - 正円ではなく、要素幅に合わせた楕円で
- 画像にフレームをつけて額縁風にする
  - `clip-path: inset();`
- 画像の端を切り落とす
  - `clip-path`によるカットアウト

## 画像の表示範囲を制御する

- 画像のどの範囲を見せるか
  - `object-view-box`
  - `aspect-ratio`

-->
